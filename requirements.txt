# All four pins installed and imported cleanly on 2026-07-27, and the full
# test suite passed against them. google-genai moves fast -- check
# https://pypi.org/project/google-genai/ before bumping.
#
# Everything here is a pure PyPI dependency; nothing needs a paid service.
#
# No google-cloud-storage: we store no audio. Voice notes stay on Telegram's
# servers and are fetched by file_id when a transcription is needed.
python-telegram-bot==22.8
google-cloud-firestore==2.28.0
google-genai==2.14.0
flask==3.1.3
